Return a JSON object with a single key "slides" whose value is a list of slide objects.
Each slide object must contain the following keys and types:
- id: string (unique per slide)
- title: string (short title for the slide)
- bullets: array of short strings (bullet points)
- visual_prompt: string (a prompt to generate an illustration or image for the slide)
- estimated_duration_sec: integer (estimated speaking time in seconds)
- speaker_notes: string (detailed speaker notes / script)

Return ONLY valid JSON. Do not include any explanatory text, markdown, or commentary.

Schema summary:
id, title, bullets, visual_prompt, estimated_duration_sec, speaker_notes

Max slides: 6

Chapter text follows (generate slides that cover the key points from the chapter):
-----
Machine learning algorithms use statistical techniques to give computer systems the ability to "learn" from data. This learning process involves identifying patterns and making decisions with minimal human intervention.

There are three main types of machine learning: supervised learning, unsupervised learning, and reinforcement learning. Each type has its own unique characteristics and applications.
-----

Produce JSON now.
